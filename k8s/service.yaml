apiVersion: v1
kind: Service
metadata:
  namespace: webapp
  name: webapp-service
spec:
  type: Loadbalancer
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: webapp
